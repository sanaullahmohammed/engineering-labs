#include<stdio.h>

void main() {
    float m, n, x;
    n = 0.000001;
    printf("Enter a number:\n");
    scanf("%f",&x);
    if(x < 1)
        printf("Invalid number\n");
    else
    {
        for(m=0; m<x; m+=n)
        
    }    
}